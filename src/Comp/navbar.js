import ReactDOM from 'react-dom';
import React, { Component } from 'react';

const navbar = props => (
  <header>
  <nav>
  <div> navbar</div>
  <div>navbar </div>
  <div> navbar </div>
  </nav>
  </header>
)

export default navbar;
